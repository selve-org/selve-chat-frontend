# SELVE Chatbot Frontend Environment Configuration
# Toll Gate Pattern - Verifies authentication from main app
# For production deployment, change ENVIRONMENT to 'production'

# ==============================================================================
# ENVIRONMENT CONTROL
# ==============================================================================
# Switch between development and production
# development: Uses localhost URLs and test keys
# production: Uses selve.me domains and live keys
ENVIRONMENT=development

# ==============================================================================
# AUTHENTICATION CONFIGURATION (Environment-based)
# ==============================================================================

# Main app URLs
MAIN_APP_URL_DEV=http://localhost:3000
MAIN_APP_URL_PROD=https://selve.me

# Chatbot URLs
CHATBOT_URL_DEV=http://localhost:4000
CHATBOT_URL_PROD=https://chat.selve.me

# Active URLs (auto-selected based on ENVIRONMENT)
NEXT_PUBLIC_MAIN_APP_URL=${MAIN_APP_URL_DEV}
NEXT_PUBLIC_CHATBOT_URL=${CHATBOT_URL_DEV}

# Clerk public key (for JWT verification only - no Clerk UI on chatbot)
# Development: Test mode
CLERK_PUBLISHABLE_KEY_DEV=your_clerk_test_publishable_key_here

# Production: Live mode
CLERK_PUBLISHABLE_KEY_PROD=your_clerk_live_publishable_key_here

# Active key (auto-selected based on ENVIRONMENT)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${CLERK_PUBLISHABLE_KEY_DEV}

# ==============================================================================
# BACKEND API CONFIGURATION (Environment-based)
# ==============================================================================

# Chatbot backend URLs
API_URL_DEV=http://localhost:9000
API_URL_PROD=https://api-chat.selve.me

# Active URL (auto-selected based on ENVIRONMENT)
NEXT_PUBLIC_API_URL=${API_URL_DEV}
NEXT_PUBLIC_API_TIMEOUT=60000

# ==============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ==============================================================================
# When deploying to production:
# 1. Change ENVIRONMENT=production (line 10)
# 2. Add your production Clerk publishable key
# 3. Verify production URLs are correct
# 4. Copy this file to .env.local
# 5. Deploy and test
